import base64
  from IPython.display import HTML

  csv_data = results['vintage_df'].to_csv(index=False)
  b64 = base64.b64encode(csv_data.encode()).decode()
  html = f'<a download="vintage_curves.csv" href="data:text/csv;base64,{b64}">Click to download vintage_curves.csv</a>'
  display(HTML(html))
